/**
 * REVIEW
 *
 * Might want to use className and customize various
 * labels.
 */

const CustomLabel = ({ label }) => (
  <label htmlFor={label}>
    <style jsx>{`
      label {
      }
    `}</style>
  </label>
)

export default CustomLabel
