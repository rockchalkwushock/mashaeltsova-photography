import WithCloudinary from './WithCloudinary'
import WithIntl from './WithIntl'

/**
 * NOTE
 *
 * Remember that compose is executing in order so the HOC's
 * are being applied left to right.
 */

export { WithCloudinary, WithIntl }
